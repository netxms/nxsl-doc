[.nxsl-function]
[[func-getserverqueuenames]]
== GetServerQueueNames

GetServerQueueNames(): <<class-array,Array>>::
Get array with server queue names.
+
.Return
Array with strings.

[.source]
....
foreach(l : GetServerQueueNames())
{
  println(l);
}
....
