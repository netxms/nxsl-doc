[.nxsl-function]
[[func-getserverqueuenames]]
== GetServerQueueNames

GetServerQueueNames(): <<class-array,Array>>::
Get array with server queue names.
+
.Return
Array with strings.

[.source]
....
for (l : GetServerQueueNames())
{
  println(l);
}
....
